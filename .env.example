# Use Postgres in production (Render injects this automatically)
DATABASE_URL=postgresql+psycopg://user:password@host:5432/london_tasteful_events

# Auto-import seed_data.json into an empty database on startup
AUTO_SEED_ORGS=true
